# tests — acceptance checks

Phase-1 acceptance criteria (SPEC §5) expressed as executable checks (containers running,
NRF registration, Registration Accept, PDU session, end-to-end ping). Populated from **M2**
onward as each capability comes online. Empty in M0/M1 by design.
