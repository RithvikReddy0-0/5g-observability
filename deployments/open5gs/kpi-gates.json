{
  "schema_version": "1",
  "_comment": [
    "KPI gates for the Open5GS stack. Same schema and the same gate tool as the free5GC set",
    "(deployments/kpi-gates.json); run with `make o5gs-gate`.",
    "",
    "Every metric below was checked against ground truth on the running stack (20 UE interfaces,",
    "10 per slice) before being trusted. Deliberately NOT used, because they read wrong:",
    "  fivegs_smffunction_sm_qos_flow_nbr        read 20/20 against 10/10 real flows — never",
    "                                            decremented when UEs re-attach",
    "  fivegs_smffunction_sm_pdusessioncreationsucc  read 40 per slice for 20 requests — counts",
    "                                            each success twice (a 200% success ratio)",
    "  fivegs_smffunction_sm_pdusessioncreationreq   also emits an unlabelled duplicate series,",
    "                                            so a plain sum() double-counts",
    "The one exact per-slice count is sm_sessionnbr (SMF, by S-NSSAI).",
    "  fivegs_upffunction_upf_sessionnbr / upf_qosflows  correct at first, then 21 sessions and 11 flows",
    "                                            for 20/10 real after ONE UE restart — lower bounds only",
    "",
    "Unlike the free5GC set, the user-plane gates here are runnable: the Open5GS UPF needs no",
    "kernel module (ADR-010).",
    "",
    "Core-side metrics cannot see UEs that disappear: with all 20 UEs stopped, every gate above",
    "`ues-reachable-per-slice` still passed. Liveness and latency come from ue_probe_exporter."
  ],
  "prometheus": "http://localhost:9091",
  "gates": [
    {
      "id": "exporters-up",
      "title": "All 5 Open5GS metric exporters are scrapeable",
      "expr": "count(up{job=\"open5gs\"} == 1)",
      "op": "gte",
      "threshold": 5,
      "unit": "NFs",
      "severity": "gate",
      "requires": "core",
      "why": "AMF, SMF, PCF and the two per-slice UPFs are the NFs that export metrics; losing one blinds the gate to that part of the network."
    },
    {
      "id": "no-scrape-failures",
      "title": "No network or exporter target is down",
      "expr": "count(up{job!=\"slice-orchestrator\"} == 0) or vector(0)",
      "op": "eq",
      "threshold": 0,
      "unit": "targets",
      "severity": "gate",
      "requires": "core",
      "why": "Includes the per-slice traffic and UE probe exporters: a broken observability plane must fail the gate rather than report a silent green. The slice orchestrator is excluded — it is an optional host process above the network, and a stopped orchestrator once failed this gate on a healthy stack; it has its own advisory."
    },
    {
      "id": "pfcp-associated",
      "title": "The SMF is associated over N4 with both slice UPFs",
      "expr": "sum(pfcp_peers_active{nf=\"smf\"})",
      "op": "gte",
      "threshold": 2,
      "unit": "peers",
      "severity": "gate",
      "requires": "core",
      "why": "One UPF per slice (ADR-011). Without a PFCP association the SMF cannot install forwarding rules for that slice, and its sessions carry nothing even if they are accepted."
    },
    {
      "id": "gnb-connected",
      "title": "Both slice gNBs are connected over N2",
      "expr": "sum(gnb)",
      "op": "gte",
      "threshold": 2,
      "unit": "gNBs",
      "severity": "gate",
      "requires": "core",
      "why": "One gNB per slice (ADR-011). A missing gNB means an entire slice has no radio access, while the other slice keeps every KPI green."
    },
    {
      "id": "ues-on-ran",
      "title": "All 20 UEs are attached",
      "expr": "sum(ran_ue)",
      "op": "gte",
      "threshold": 20,
      "unit": "UEs",
      "severity": "gate",
      "requires": "core",
      "why": "Matched the 20 UE interfaces exactly in the ground-truth audit, unlike free5GC's AMF gauges."
    },
    {
      "id": "pdu-sessions-at-upf",
      "title": "Every UE has a user-plane session at the UPF",
      "expr": "sum(fivegs_upffunction_upf_sessionnbr)",
      "op": "gte",
      "threshold": 20,
      "unit": "sessions",
      "severity": "gate",
      "requires": "core",
      "why": "Counted where traffic is carried, not where it is signalled: a session the UPF does not hold cannot forward a packet (ODE-only on free5GC). A LOWER bound only: after one UE was restarted the UPF reported 21 sessions for 20 real ones while the SMF correctly reported 20, so this gauge can over-count and must never be used as an exact total."
    },
    {
      "id": "upf-sessions-per-slice",
      "title": "Each slice's own UPF holds sessions",
      "expr": "count(fivegs_upffunction_upf_sessionnbr > 0)",
      "op": "gte",
      "threshold": 2,
      "unit": "UPFs",
      "severity": "gate",
      "requires": "core",
      "why": "With a UPF per slice, the UPF's native session gauge becomes per-slice. Presence only: the gauge over-counts after UE restarts, so it is never used as an exact count."
    },
    {
      "id": "sessions-per-slice",
      "title": "Each slice holds its 10 PDU sessions",
      "expr": "min(fivegs_smffunction_sm_sessionnbr)",
      "op": "gte",
      "threshold": 10,
      "unit": "sessions",
      "severity": "gate",
      "requires": "core",
      "why": "min() over the per-S-NSSAI series: a total of 20 would still pass if one slice had all of them and the other none."
    },
    {
      "id": "both-slices-have-sessions",
      "title": "Both slices appear in the SMF's slice-labelled metrics",
      "expr": "count(fivegs_smffunction_sm_sessionnbr > 0)",
      "op": "gte",
      "threshold": 2,
      "unit": "slices",
      "severity": "gate",
      "requires": "core",
      "why": "Guards sessions-per-slice against a slice vanishing from the metrics entirely, where min() over one remaining series would still look healthy."
    },
    {
      "id": "user-plane-flows-per-slice",
      "title": "The UPF holds QoS flows for both slices' DNNs",
      "expr": "count(fivegs_upffunction_upf_qosflows > 0)",
      "op": "gte",
      "threshold": 2,
      "unit": "DNNs",
      "severity": "gate",
      "requires": "core",
      "why": "Each slice has its own DNN (internet = eMBB, urllc = URLLC), so this confirms slice separation inside the user plane. Counts presence per DNN only: after a UE restart the UPF reported 11 flows on 'internet' for 10 real sessions."
    },
    {
      "id": "slice-traffic-exporter",
      "title": "Both slices' UPF devices are measurable",
      "expr": "count(upf_slice_up == 1)",
      "op": "gte",
      "threshold": 2,
      "unit": "slices",
      "severity": "gate",
      "requires": "core",
      "why": "Native UPF volume counters are labelled only by QFI and cannot separate slices; per-slice throughput depends entirely on this exporter."
    },
    {
      "id": "ues-reachable-per-slice",
      "title": "Every UE on each slice answered through the UPF within 30 s",
      "expr": "min(max_over_time(ue_probe_reachable[30s]))",
      "op": "gte",
      "threshold": 10,
      "unit": "UEs",
      "severity": "gate",
      "requires": "core",
      "why": "The only liveness signal measured from the UE side: with every UE stopped, the AMF still reported 20 UEs and the UPF 20 sessions. Windowed over 30 s because one probe lost in a single 5 s round is not a dead UE (URLLC lost up to 4 of 10 probes per round under eMBB load with no UE actually down); a UE that is really gone never reaches 10, and the UE supervisor restarts such a UE within ~30 s."
    },
    {
      "id": "urllc-latency-budget",
      "title": "URLLC average round-trip time within its 10 ms budget (1 min)",
      "expr": "max(avg_over_time(ue_probe_rtt_ms{slice=\"URLLC\",stat=\"avg\"}[1m]))",
      "op": "lte",
      "threshold": 10,
      "unit": "ms",
      "severity": "gate",
      "requires": "core",
      "why": "SLICE_B_MAX_LATENCY_MS in slices.env, measured UE -> gNB -> UPF on the slice's own path. Enforced on the average: measured 2.9-3.4 ms idle and under eMBB load. The worst case is reported separately as advisory because it already exceeds 10 ms at idle on this host."
    },
    {
      "id": "embb-latency-budget",
      "title": "eMBB worst round-trip time within its 300 ms budget (1 min)",
      "expr": "max(max_over_time(ue_probe_rtt_ms{slice=\"eMBB\",stat=\"max\"}[1m]))",
      "op": "lte",
      "threshold": 300,
      "unit": "ms",
      "severity": "gate",
      "requires": "core",
      "why": "SLICE_A_MAX_LATENCY_MS in slices.env, measured the same way as URLLC."
    },
    {
      "id": "urllc-tail-latency",
      "title": "URLLC worst-case round-trip time within 10 ms (1 min)",
      "expr": "max(max_over_time(ue_probe_rtt_ms{slice=\"URLLC\",stat=\"max\"}[1m]))",
      "op": "lte",
      "threshold": 10,
      "unit": "ms",
      "severity": "advisory",
      "requires": "core",
      "why": "Advisory, and expected to WARN on this laptop: the worst probe of a minute measured 10.14 and 10.95 ms at IDLE in two runs. A software UPF and simulated RAN sharing a laptop CPU cannot hold a URLLC tail budget; this states that plainly instead of hiding it behind an average."
    },
    {
      "id": "urllc-no-probe-loss",
      "title": "No URLLC probe lost in the last minute",
      "expr": "min(min_over_time(ue_probe_reachable{slice=\"URLLC\"}[1m]))",
      "op": "gte",
      "threshold": 10,
      "unit": "UEs",
      "severity": "advisory",
      "requires": "core",
      "why": "Advisory: the slice-isolation signal. With eMBB saturated at 350-470 Mbps, URLLC lost up to 4 of 10 probes per round (idle: none) while its average RTT did not change — the slices share fate through packet loss, not delay."
    },
    {
      "id": "no-registration-failures",
      "title": "No UE registration failures in the last 5 minutes",
      "expr": "sum(increase(fivegs_amffunction_rm_reginitfail[5m])) or vector(0)",
      "op": "eq",
      "threshold": 0,
      "unit": "failures",
      "severity": "advisory",
      "requires": "core",
      "why": "Advisory: failures are expected for a few minutes after the Docker engine restarts underneath running UEs, which recover on retry."
    },
    {
      "id": "slices-carrying-traffic",
      "title": "Both slices carried user traffic in the last 5 minutes",
      "expr": "count(increase(upf_slice_uplink_packets_total[5m]) > 0)",
      "op": "gte",
      "threshold": 2,
      "unit": "slices",
      "severity": "advisory",
      "requires": "core",
      "why": "Advisory: an idle network is not a broken one. Reported so that a demo or test run shows traffic genuinely reached both slices."
    },
    {
      "id": "orchestrator-reachable",
      "title": "The slice orchestrator is being scraped",
      "expr": "max(up{job=\"slice-orchestrator\"})",
      "op": "gte",
      "threshold": 1,
      "unit": "up",
      "severity": "advisory",
      "requires": "core",
      "why": "Advisory: demand admission is unavailable while it is down, but the network itself is unaffected. Start it with scripts/open5gs/start_orchestrator.sh."
    }
  ]
}
